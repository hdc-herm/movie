server:
  port: 8002
spring:
  application:
    name: cloud-movie-client
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        prefix: cloud-server-dbconfig
